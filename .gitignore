# 依赖
node_modules

# VitePress 构建产物与缓存
docs/.vitepress/cache
docs/.vitepress/dist

# VitePress dev 时生成的临时配置文件
docs/.vitepress/config.mts.timestamp-*.mjs

# macOS 元数据
.DS_Store

# 本地 AI 工具配置（不发布）
.workbuddy/
.claude/

# 构建时自动重新生成（docs:dev / docs:build 都会跑 zip:template）
docs/public/downloads/template.zip
